# Copy to .env.local and fill in from the Supabase dashboard (Project Settings → API).
# Only the anon key ever appears here — service-role and Twilio credentials live in
# Edge Function secrets, never in VITE_ variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
